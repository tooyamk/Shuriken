execute_process(COMMAND git -C ${SRC_DIR} clean -xfd)
execute_process(COMMAND git -C ${SRC_DIR} reset --hard)

set(file ${SRC_DIR}/CMakeLists.txt)
file(READ ${file} content)
string(REPLACE "set(CMAKE_DEBUG_POSTFIX \"d\")" "" content "${content}")
string(REPLACE "\"1.dll\"" "\"${SRK_DLL_SUFFIX}\"" content "${content}")
string(REPLACE "set_target_properties(zlib PROPERTIES SOVERSION 1)" "" content "${content}")
string(REPLACE "set_target_properties(zlib PROPERTIES VERSION \${ZLIB_FULL_VERSION})" "" content "${content}")
string(REGEX REPLACE "Example binaries.+" "" content "${content}")
file(WRITE ${file} "${content}")